extends ../layouts/base-sidebar.pug

block config
    - var bodyClass = 'sb-nav-fixed'
    - var pageTitle = 'Extra';
    - var index = true;
    
block content 
  .pure-u-1
      #extras
        each item, i in extras
          .card
            h3.cardTitle= item.title
            a.journalLink(href=item.link)= item.linkText
            p.cardInfo= item.additionalInfo


